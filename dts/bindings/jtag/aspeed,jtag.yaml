description: ASPEED family JTAG

compatible: "aspeed,jtag"

include: [base.yaml, pinctrl-device.yaml, reset-device.yaml]

properties:
    reg:
      required: true

    interrupts:
      required: true
